# 🎯 คลังข้อสอบจำลอง Embedded AI: ชุดข้อสอบปรนัยระดับ Advance (ข้อ 21–35)
> **รหัสวิชา**: 89037467 (Introduction to Embedded AI) | ผู้สอน: อ.ดร.พลวัต ช่อผูก  
> **ฉบับเจาะลึกเฉพาะทาง**: เน้นการคำนวณจริง, ตัวเลขสเปกฮาร์ดแวร์, การวิเคราะห์สถาปัตยกรรมระดับลึก และโจทย์ประยุกต์เชิงวิศวกรรม Edge AI สำหรับเตรียมสอบ

---

## ⚡ ชุดข้อสอบปรนัยระดับ Advance (ข้อ 21–35)

### 21. ในสถาปัตยกรรมหน่วยความจำแบบ 64-bit Address Bus หากระบบคอมพิวเตอร์รองรับการเข้าถึงข้อมูลแบบ Byte-Addressable ปริมาณ Addressable Memory สูงสุดตามทฤษฎีคือเท่าใด?
* ก. 16 Exabytes ($16 \times 2^{60}\text{ Bytes}$)
* ข. 4 Gigabytes ($4 \times 2^{30}\text{ Bytes}$)
* ค. 18.44 Exabytes ($2^{64}\text{ Bytes}$)
* ง. 64 Terabytes ($64 \times 2^{40}\text{ Bytes}$)

### 22. กล้องวงจรปิดอัจฉริยะประมวลผลภาพด้วยโมเดล Detection ที่ต้องการคำนวณ $45\text{ GMACs}$ ต่อเฟรม โดยต้องการให้รันที่ความเร็ว $25\text{ FPS}$ แบบ Real-time ถามว่าโมเดลนี้ต้องการพลังการประมวลผลขั้นต่ำเทียบเท่ากี่ TFLOPs และหากต้องเลือก NPU (INT8) ที่มี Hardware Efficiency $60\%$ ต้องเลือกรุ่นที่มีสเปกขั้นต่ำกี่ TOPS?
* ก. 1.125 TFLOPs และ NPU ขั้นต่ำ 1.875 TOPS
* ข. 2.250 TFLOPs และ NPU ขั้นต่ำ 3.750 TOPS
* ค. 2.250 TFLOPs และ NPU ขั้นต่ำ 2.250 TOPS
* ง. 4.500 TFLOPs และ NPU ขั้นต่ำ 7.500 TOPS

### 23. โมเดล CNN ชั้นหนึ่งรับ Feature Map ขาเข้าขนาด $56 \times 56 \times 64$ ($W \times H \times C$) ผ่านเลเยอร์ Conv2D ที่มี Kernel ขนาด $3 \times 3$, Padding $P=1$, Stride $S=2$ จำนวน 128 ฟิลเตอร์ และต่อด้วย Max Pooling ขนาด $2 \times 2$ (Stride $S=2$) จงหามิติของ Feature Map หลังผ่านทั้งสองชั้นตามลำดับ?
* ก. Conv2D: $28 \times 28 \times 128$ และ MaxPool: $14 \times 14 \times 128$
* ข. Conv2D: $56 \times 56 \times 128$ และ MaxPool: $28 \times 28 \times 128$
* ค. Conv2D: $28 \times 28 \times 64$ และ MaxPool: $14 \times 14 \times 64$
* ง. Conv2D: $27 \times 27 \times 128$ และ MaxPool: $13 \times 13 \times 128$

### 24. จงคำนวณจำนวน Parameters รวมทั้งหมดของเลเยอร์ Conv2D ที่ใช้ Kernel ขนาด $3 \times 3$, Input Channel = 64, Output Channel = 128 (มี Bias ทุกฟิลเตอร์) และคำนวณจำนวนการคำนวณ FLOPs ในการทำ Forward Pass 1 ครั้งบน Feature Map ขนาดขาออก $28 \times 28$?
* ก. Params = 73,856 ตัว, FLOPs $\approx 115.8\text{ MFLOPs}$
* ข. Params = 73,728 ตัว, FLOPs $\approx 57.9\text{ MFLOPs}$
* ค. Params = 73,856 ตัว, FLOPs $\approx 57.9\text{ MFLOPs}$
* ง. Params = 36,928 ตัว, FLOPs $\approx 28.9\text{ MFLOPs}$

### 25. ในการออกแบบสถาปัตยกรรม CNN เหตุใดการใช้ Convolution ขนาด $3 \times 3$ ซ้อนกัน 2 ชั้น จึงมีประสิทธิภาพและเป็นที่นิยมมากกว่าการใช้ Convolution ขนาด $5 \times 5$ เพียงชั้นเดียว (เมื่อกำหนดให้ Stride=1 และ Channel เท่ากัน)?
* ก. เพราะ $3 \times 3$ ซ้อนกัน 2 ชั้น ให้ Receptive Field $6 \times 6$ ซึ่งกว้างกว่า $5 \times 5$
* ข. เพราะประหยัดพารามิเตอร์ลงได้ $28\%$ และมีฟังก์ชัน Non-linear Activation (เช่น ReLU) คั่น 2 รอบ ทำให้เรียนรู้ฟีเจอร์ซับซ้อนได้ดีกว่า
* ค. เพราะเลเยอร์ $3 \times 3$ ไม่ต้องใช้หน่วยความจำในการคำนวณ Padding
* ง. เพราะการใช้ $5 \times 5$ ชั้นเดียวจะทำให้เกิดปัญหา Dying ReLU เสมอ

### 26. ในโหนดของ Hidden Layer หากอินพุตมีค่า $x_1 = 3.0, x_2 = -4.0$ โดยมีค่าน้ำหนัก $w_1 = 0.5, w_2 = 0.8$ และมีค่า Bias $b = -0.3$ หากฟังก์ชันกระตุ้น (Activation Function) เป็น Leaky ReLU ที่มีค่าความชันฝั่งลบ $\alpha = 0.05$ ผลลัพธ์เอาต์พุตของโหนดนี้จะมีค่าเท่าใด?
* ก. 0.000
* ข. -2.000
* ค. -0.100
* ง. 0.100

### 27. เหตุใดในการเทรนโครงข่ายประสาทเทียมแบบ Deep Neural Networks จึงนิยมใช้ฟังก์ชันกระตุ้นแบบ ReLU หรือ SiLU ใน Hidden Layers มากกว่าฟังก์ชัน Sigmoid หรือ Tanh?
* ก. เพราะ Sigmoid ไม่สามารถคำนวณอนุพันธ์ (Derivative) ได้
* ข. เพราะ Sigmoid และ Tanh ทำให้เกิดปัญหา Vanishing Gradient ในชั้นที่ลึก เนื่องจากความชันมีค่าลู่เข้าใกล้ 0 เมื่อค่าอินพุตมีขนาดบวกหรือลบมากๆ
* ค. เพราะ ReLU ให้ค่าเอาต์พุตที่มีขอบเขตจำกัดระหว่าง 0 ถึง 1 ช่วยให้ประหยัดแรม
* ง. เพราะ SiLU เป็นฟังก์ชันเชิงเส้น (Linear Function) จึงคำนวณได้เร็วกว่า Sigmoid

### 28. ในการทำ Batch Normalization (BatchNorm) เพื่อเร่งความเร็วในการฝึกโมเดล Deep Learning ข้อใดอธิบายการทำงานในโหมด Training เทียบกับโหมด Inference (การใช้งานจริงบนบอร์ด Edge) ได้ถูกต้องที่สุด?
* ก. ในโหมด Inference จะคำนวณ Mean และ Variance สดใหม่จาก Batch ปัจจุบันเหมือนในโหมด Training
* ข. ในโหมด Inference จะใช้ค่า Running Mean และ Running Variance ที่สะสมและตรึงไว้จากการเทรน ทำให้ไม่ต้องรวมสถิติทั้ง Batch และสามารถรวมน้ำหนัก (Fold) เข้ากับ Conv Layer ก่อนหน้าได้
* ค. ในโหมด Inference จะปิดการทำงานของ BatchNorm โดยการปรับค่า Scale ($\gamma$) และ Shift ($\beta$) ให้เป็น 0 ทั้งหมด
* ง. BatchNorm ไม่สามารถรันบนบอร์ด Edge ได้ ต้องถอดทิ้งเสมอก่อนคอมไพล์

### 29. ในสถาปัตยกรรม YOLOv5 บล็อก C3 (Cross-Stage Partial with 3 Convolutions) มีสมการการทำงาน $Y = \text{Conv}_3\left( [ B_N(\text{Conv}_1(X)) \parallel \text{Conv}_2(X) ] \right)$ ข้อใดระบุบทบาทของสาย $\text{Conv}_2(X)$ ได้ถูกต้องที่สุด?
* ก. ทำหน้าที่เป็น Short-cut/Residual Bypass ช่วยส่งผ่าน Gradient ดั้งเดิมโดยไม่ผ่านคอขวด ทำให้โมเดลลึกไม่ติดหล่มและลดภาระการคำนวณ
* ข. ทำหน้าที่ลดขนาดมิติภาพลงครึ่งหนึ่งแทนการใช้ Max Pooling
* ค. ทำหน้าที่คำนวณ Softmax เพื่อจัดกลุ่มคลาสของวัตถุ
* ง. ทำหน้าที่คูณกับค่า Matrix ของ Self-Attention

### 30. กลไก SPPF (Spatial Pyramid Pooling - Fast) ใน YOLOv5 ให้ผลลัพธ์ Feature Representation เทียบเท่ากับ SPP ดั้งเดิมใน YOLOv4 แต่เหตุใด SPPF จึงทำงานได้เร็วกว่าอย่างมีนัยสำคัญ?
* ก. เพราะ SPPF ตัด MaxPool ขนาด $13 \times 13$ ทิ้งไป
* ข. เพราะ SPPF นำ MaxPool ขนาดเล็ก $5 \times 5$ มาต่อเรียงกันแบบอนุกรม ($5 \to 9 \to 13$) ทำให้ใช้หน่วยความจำแคชซ้ำและลด Memory Access Overhead เมื่อเทียบกับการรัน MaxPool ขนาดใหญ่ 5, 9, 13 แยกขนานกัน
* ค. เพราะ SPPF ทำงานบนโดเมนความถี่โดยใช้ Fast Fourier Transform (FFT)
* ง. เพราะ SPPF รันเฉพาะบน Tensor Core เท่านั้น

### 31. ในโมเดล Object Detection ตระกูล YOLO โมเดลจะสร้าง Anchor Boxes ออกมาจาก Feature Pyramid ทั้งหมด 3 ระดับ ($P_3, P_4, P_5$) หากขนาดภาพอินพุตคือ $640 \times 640$ พิกเซล เลเยอร์ $P_3$ (Stride = 8) มีหน้าที่หลักในการตรวจจับวัตถุประเภทใด และมีขนาดกริดเท่าใด?
* ก. ตรวจจับวัตถุขนาดใหญ่มาก, กริดขนาด $20 \times 20$
* ข. ตรวจจับวัตถุขนาดกลาง, กริดขนาด $40 \times 40$
* ค. ตรวจจับวัตถุขนาดเล็ก, กริดขนาด $80 \times 80$
* ง. ตรวจจับฉากหลัง (Background), กริดขนาด $160 \times 160$

### 32. หากนำบอร์ด Raspberry Pi 4 (แรม 4GB) มารันโมเดล YOLOv8n เพื่อทำ Object Detection ตรวจจับคนและหมวกนิรภัยแบบ Real-time แต่พบปัญหาบอร์ดร้อนสะสมจนแตะ $82^\circ\text{C}$ ทำให้เฟรมเรตร่วงจาก 3 FPS เหลือเพียง 1.2 FPS วิศวกรควรแก้ปัญหานี้อย่างไรให้ตรงจุดที่สุดตามหลักวิศวกรรม Edge AI?
* ก. ติดตั้งชุดระบายความร้อน Active Cooling (พัดลม + Heatsink) และเปลี่ยนโมเดลเป็น SSD MobileNet v2 หรือต่อ Google Coral USB เพื่อ Offload ภาระคำนวณ
* ข. เพิ่มขนาดไฟล์ Swap บน MicroSD Card ให้เป็น 16 GB
* ค. แก้ไขไฟล์ `cmdline.txt` โดยเพิ่มตัวแปร `force_turbo=1`
* ง. โอเวอร์คล็อกความถี่ CPU BCM2711 จาก 1.5 GHz เป็น 2.2 GHz

### 33. ข้อใดคือความแตกต่างทางสถาปัตยกรรมระหว่าง "Google Coral Edge TPU" และ "Raspberry Pi AI HAT+ (ชิป Hailo-8)" ที่วิศวกรต้องคำนึงถึงในการเลือกใช้งาน?
* ก. Coral Edge TPU รองรับทั้ง FP32 และ INT8 แต่ Hailo-8 รองรับเฉพาะ Binary Weights
* ข. Coral Edge TPU เชื่อมต่อผ่าน USB 3.0 ให้พลัง 4 TOPS (INT8 เท่านั้น) ขณะที่ Hailo-8 ต่อผ่านบัส PCIe 2.0/3.0 ให้พลังสูงถึง 26 TOPS และรองรับโมเดลโครงสร้างซับซ้อนได้ยืดหยุ่นกว่า
* ค. Coral Edge TPU กินไฟ 25W ขณะที่ Hailo-8 กินไฟต่ำกว่า 0.5W
* ง. Coral Edge TPU ต้องเขียนโค้ดด้วยภาษา C++ เท่านั้น ส่วน Hailo-8 รองรับเฉพาะ Python

### 34. ในโมเดลภาษาขนาดใหญ่ (LLM) ปัญหาหลักที่ทำให้การ Generate ข้อความบนอุปกรณ์ Edge มีความเร็วจำกัด (Tokens/sec ต่ำ) ไม่ได้เกิดจากการขาดแคลนพลังคำนวณดิบ (Compute-bound) เพียงอย่างเดียว แต่เกิดจาก "Memory Bandwidth Bound" ปรากฏการณ์นี้เกิดขึ้นจากสาเหตุใด?
* ก. ค่าความจุแบตเตอรี่จ่ายกระแสไม่ทันในจังหวะสุ่มคำ Softmax
* ข. ชิปไม่รองรับการคำนวณฟังก์ชันตรีโกณมิติของ Positional Encoding
* ค. ในการสร้างข้อความทีละ 1 Token ระบบจำเป็นต้องอ่านค่าน้ำหนักพารามิเตอร์ทั้งหมดของโมเดลหลายพันล้านตัวจาก DRAM ผ่านบัสหน่วยความจำเข้ามายังแคชในชิป ทำให้ความเร็วถูกจำกัดด้วยความเร็วบัสของแรม
* ง. เกิดจากการทำงานของคีย์บอร์ดเสมือน Squeekboard บนหน้าจอสัมผัส

### 35. อุปกรณ์ตรวจสอบการสั่นสะเทือนของมอเตอร์อุตสาหกรรม (Vibration Predictive Maintenance) ต้องติดตั้งบนตัวเครื่องจักรในจุดที่ไม่มีปลั๊กไฟ ต้องใช้แบตเตอรี่ก้อนเล็กและทำงานต่อเนื่อง 3 ปี โดยมีโมเดล TinyML ทำการประมวลผลสัญญาณ FFT และทำนายความผิดปกติทุกๆ 1 นาที บอร์ดหรือชิปใดต่อไปนี้เหมาะสมที่สุดทั้งในแง่ของสถาปัตยกรรมและงบประมาณพลังงาน?
* ก. NVIDIA Jetson AGX Orin (60W)
* ข. Raspberry Pi 5 ต่อการ์ด AI HAT+ (15W)
* ค. STM32N6 (STM32N657 MCU มี Neural-ART NPU ในตัว, กินไฟ < 1W, รองรับ Deep Sleep)
* ง. Orange Pi 5 (Rockchip RK3588, 15W)

---

## 🔑 เฉลยคำตอบข้อสอบปรนัยชุด Advance พร้อมคำอธิบายละเอียด (ข้อ 21–35)

21. **เฉลย ค.** คำนวณจากสูตร $2^N = 2^{64}\text{ Bytes} \approx 1.844 \times 10^{19}\text{ Bytes} = \mathbf{18.44\text{ Exabytes (EB)}}$ (โดยที่ $1\text{ EB} = 2^{60}\text{ Bytes}$)
22. **เฉลย ข.**
    * $1\text{ MAC} = 2\text{ FLOPs} \implies 45\text{ GMACs} = 90\text{ GFLOPs ต่อเฟรม}$
    * รันที่ 25 FPS: $\frac{90 \times 25}{1,000} = \frac{2,250}{1,000} = \mathbf{2.250\text{ TFLOPs}}$
    * ประสิทธิภาพ $60\%$: $\text{Required NPU Spec} = \frac{2.250}{0.60} = \mathbf{3.750\text{ TOPS}}$
23. **เฉลย ก.**
    * Conv2D: $W = \lfloor \frac{56 - 3 + 2(1)}{2} \rfloor + 1 = \lfloor \frac{55}{2} \rfloor + 1 = 27 + 1 = 28 \implies \mathbf{28 \times 28 \times 128}$
    * MaxPool: $W = \frac{28}{2} = 14 \implies \mathbf{14 \times 14 \times 128}$
24. **เฉลย ค.**
    * $\text{Params} = (3 \times 3 \times 64 + 1) \times 128 = (576 + 1) \times 128 = 577 \times 128 = \mathbf{73,856\text{ ตัว}}$
    * $\text{FLOPs} = 2 \times (3 \times 3 \times 64) \times 128 \times (28 \times 28) \approx 2 \times 576 \times 128 \times 784 \approx \mathbf{57.9\text{ MFLOPs}}$ (หรือประมาณ 115.8 M Operations หากนับบวก Bias)
25. **เฉลย ข.**
    * Receptive Field รวมของ $3 \times 3$ สองชั้นคือ $1 + (3-1) + (3-1) = 5 \times 5$ เท่ากับเลเยอร์ $5 \times 5$ ชั้นเดียว
    * จำนวนพารามิเตอร์: $2 \times (3 \times 3) = 18$ เทียบกับ $1 \times (5 \times 5) = 25$ ประหยัดลง $28\%$ และมี Non-linearity สองรอบช่วยเพิ่มความสามารถในการแยกแยะฟีเจอร์
26. **เฉลย ค.**
    * คำนวณผลรวมเชิงเส้น: $z = (w_1 x_1) + (w_2 x_2) + b = (0.5 \times 3.0) + (0.8 \times -4.0) + (-0.3) = 1.5 - 3.2 - 0.3 = \mathbf{-2.0}$
    * ผ่าน Leaky ReLU: เพราะ $z < 0 \implies f(z) = \alpha z = 0.05 \times (-2.0) = \mathbf{-0.100}$
27. **เฉลย ข.** ฟังก์ชัน Sigmoid และ Tanh มีอนุพันธ์สูงสุดเพียง 0.25 และ 1.0 ซึ่งเมื่อค่า $z$ สูงหรือต่ำมากๆ อนุพันธ์จะกลายเป็น 0 เมื่อใช้ Backpropagation คูณกันหลายๆ เลเยอร์ ค่า Gradient จะเล็กลงจนหายไป (Vanishing Gradient) ทำให้เลเยอร์แรกๆ ไม่เรียนรู้
28. **เฉลย ข.** ในโหมด Inference ค่าสถิติ $\mu$ และ $\sigma^2$ จะถูกล็อกตายตัวจาก Running Average ของชุดเทรน ทำให้สามารถยุบค่าน้ำหนักรวมเข้ากับ Convolutional Weight ก่อนหน้าได้เลย (Conv-BatchNorm Fusion) ช่วยลดเวลาและหน่วยความจำบน Edge ได้มหาศาล
29. **เฉลย ก.** สาย $\text{Conv}_2(X)$ ในโครงสร้าง CSP คือเส้นทางลัดข้าม (Bypass Pathway) ที่แบ่งสัญญาณครึ่งหนึ่งส่งตรงไปยังจุด Concat ช่วยรักษาฟีเจอร์เดิม ป้องกัน Gradient หาย และลดการคำนวณในบล็อก Bottleneck ลงครึ่งหนึ่ง
30. **เฉลย ข.** SPP เดิมต้องคำนวณ MaxPool ขนาดใหญ่ $13 \times 13$ และ $9 \times 9$ ซึ่งกินเวลาในการดึงข้อมูลจากแรมมาก แต่ SPPF ทำ MaxPool ขนาดเล็ก $5 \times 5$ ซ้ำกัน 3 รอบ ข้อมูลจะวนอยู่ใน L1/L2 Cache ทำให้เร็วกว่า 2 เท่าโดยได้ผลลัพธ์เชิงตัวเลขตรงกัน
31. **เฉลย ค.** เลเยอร์ $P_3$ มีขนาด Stride เพียง 8 ($640 / 8 = 80 \times 80$) เป็น Feature Map ที่ละเอียดที่สุดในบรรดา 3 สเกล จึงมีหน้าที่ในการตรวจจับ **วัตถุขนาดเล็ก (Small Objects)**
32. **เฉลย ก.** ปัญหาเกิดจาก Thermal Throttling เมื่อความร้อนเกิน 80°C ซีพียูจะลดความเร็วลงเพื่อความปลอดภัย การแก้ที่ถูกจุดคือต้องติดพัดลมระบายความร้อน และลดภาระงานของ CPU โดยเปลี่ยนไปใช้โมเดลเบา (SSD MobileNet) หรือต่อ Google Coral เพื่อ Offload การคำนวณ
33. **เฉลย ข.** Google Coral ใช้ชิป Edge TPU เชื่อมต่อผ่าน USB 3.0 ให้พลัง 4 TOPS ที่ INT8 เท่านั้น ในขณะที่ Hailo-8 บน Raspberry Pi AI HAT+ เชื่อมผ่านบัส PCIe ให้พลังประมวลผลสูงถึง 26 TOPS และมีสถาปัตยกรรมโครงข่ายประสาทเทียมที่ยืดหยุ่นกว่ามาก
34. **เฉลย ค.** ในกระบวนการ Autoregressive ของ LLM การทำนายแต่ละ Token ต้องดึงน้ำหนักทุกตัวของโมเดล (เช่น 7 พันล้านตัว) จาก DRAM วิ่งผ่าน Bus เข้า Core ส่งผลให้ความเร็วถูกล็อกไว้ที่ขีดจำกัดของ Memory Bandwidth ไม่ใช่พลังคำนวณของ ALU/GPU
35. **เฉลย ค.** บอร์ด STM32N6 เป็นไมโครคอนโทรลเลอร์ (MCU) สถาปัตยกรรม ARM Cortex-M55 พร้อม Neural-ART NPU กินไฟไม่ถึง 1–2 วัตต์ และมีโหมด Low-Power Sleep กินไฟระดับไมโครแอมป์ จึงเป็นตัวเลือกเดียวที่สามารถทำงานบนแบตเตอรี่ได้ต่อเนื่องยาวนานเป็นปีตามโจทย์กำหนด
